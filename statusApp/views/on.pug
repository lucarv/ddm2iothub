extends layout

block content                        
        .panel.panel-default
            .panel-heading #{title}
            .panel-body    
                each device in devices
                    li #{device.deviceId}